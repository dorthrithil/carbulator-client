import {ConditionalPluralPipe} from './conditional-plural.pipe';

describe('ConditionalPluralPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionalPluralPipe();
    expect(pipe).toBeTruthy();
  });
});
